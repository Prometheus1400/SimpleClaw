[package]
name = "grep_tool"
version = "0.1.0"
edition = "2021"

[dependencies]
glob = "0.3"
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sandbox_common = { path = "../common" }
